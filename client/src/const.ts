// Client-wide constants. Nothing here yet — this file exists so future
// shared values (feature flags, route names, etc.) have an obvious home
// instead of getting scattered across components.
export {};
