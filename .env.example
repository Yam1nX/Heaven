# --- AI Room Guide (server-side vision-capable LLM call) ---
# Any OpenAI-compatible provider works. Base URL/model are optional overrides.
OPENAI_API_KEY=
OPENAI_API_BASE_URL=
OPENAI_MODEL=

# --- Optional analytics (only injected if BOTH are set — see client/index.html) ---
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=
